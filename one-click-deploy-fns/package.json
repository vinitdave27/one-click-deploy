{
	"name": "one-click-deploy-fns",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "twilio-run",
		"deploy": "twilio-run deploy --username $TWILIO_ACCOUNT_SID --password $TWILIO_AUTH_TOKEN --override-existing-project --production"
	},
	"dependencies": {
		"@twilio/runtime-handler": "1.2.4",
		"twilio": "^3.56"
	},
	"devDependencies": {
		"twilio-run": "^3.4.5"
	},
	"engines": {
		"node": "14"
	}
}
