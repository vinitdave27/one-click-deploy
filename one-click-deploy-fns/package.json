{
	"name": "one-click-deploy-fns",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "twilio-run",
		"deploy": "twilio-run deploy --username $TWILIO_ACCOUNT_SID --password $TWILIO_AUTH_TOKEN --override-existing-project --environment=prod"
	},
	"dependencies": {
		"twilio": "^3.56",
		"@twilio/runtime-handler": "1.2.4"
	},
	"devDependencies": {
		"twilio-run": "^3.4.4"
	},
	"engines": {
		"node": "14"
	}
}
