pipeline {
  agent any
  
  stages {
    stage("build") {
      echo "this is build"
    }
    stage("test") {
      echo "this is test"
    }
    stage("deploy") {
      echo "this is deploy"
    }
  }
}
